'use client';

import { Button } from '@/components/ui/button';

export default function AddToWatchlist() {
    return (
        <Button className="bg-white text-purple-700 font-semibold rounded-full px-4 py-2 shadow-md hover:bg-purple-100 w-60">
            + Add to Your Watchlist
        </Button>
    );
}
